// I started writing the setup function
function setup() {

  // Then, I created a cavas (500px wide and 300px high)
  createCanvas(500, 300);

  //I declared the brackground color (203)
  background(203);
}


// Declare the draw() function
function draw(){

  // I used the text function to write "Hello World!"
  text("Hello World!", 200, 150);
}
