var clicks = 3;

function setup() {
  createCanvas( windowWidth, windowHeight);

}

function draw() {
  background(200);
  var quanty = map(mouseY, 0, height, 20, 60);

  if (option == 1) {
    for (var x = 50; x)
  }

}
